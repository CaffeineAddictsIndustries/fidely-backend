Now lets creed a db seed so that we can localy test the current and new features we will implement;
- Create a store_admin with a username test and password test, use the service created previously to generate the hash to save in the db.
 - The store_admin should have 20 stores.
 - 