# Environment variables for local development
# Use host.docker.internal when running Go from WSL with Docker Desktop
DATABASE_URL=postgres://fidely:fidely@host.docker.internal:5432/fidely?sslmode=disable
SERVER_PORT=8080
